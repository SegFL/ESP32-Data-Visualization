

Al tener elmodo curva ON al principio generaba un corto a la salida, hay alguna variable que al
iniciar el sistema hace que el DC sea 1
El error se fue al activar/desactivar elmodo curva

Corregir elhecho de que se pone en corto la salida al reiniciarse elmicro

No funciona el limite maximo de corriente en modo manual:
    Seguir por aca,guardar el ultimo duty valido, cuando imeas supere elmaximo se pone ese duty, cada vez
    que no supere elmaximodeberia guardar el valor de duty en safedutycycle
      float imeas = getLastCurrentData(); // Ultima corriente medida por el sensor en mA
        if(imeas>maxCurrent){
            dutyCycleAux=lastSageDutyCycle; //Si la corriente medida supera el maximo permitido se mantiene el ultimo duty cycle seguro
            writeSerialComln(String("Corriente medida supera el máximo permitido. Duty Cycle ajustado al ultimo valor seguro."));
        }


Agregar pesta;a para crear curvas y enviarlas al esp32

Agregar menu para ver curva asociada a principio

Agregar curva de calibracion para cuando la carga electronica no tenga elpid activo. Modificar convertirCorrienteADc()

Corregir el hecho de que la carga entre en corto al iniciar el uC, ahora que el WiFi es mas lento
el corto dura mas, puede ser por alguna salida en 0, usar primero el Init de las salidas, ejecutarlo primero.
  en carga electronica.cpp linea 54 se iniciliza el pwm en 0, capas hay que inicializarlo en elmaximo


8/2/2026

Falta corregir el hecho de que se pierdan 4 incrementos en la interpolacion lineal.
Falta ahcer varias pruebas con los nuevos cambios del modolineal.



12/2/2026

bool loadCurveNVS(const char* key):
  No agranda el curveArray si no hay lugar. Mas adletnate deberia agregar posiciones al arreglo

Cada vez que se envie la impresion de modulos se deberian enviar almenso 7 /n/r 
para que en la app no se muestre todo seguido


Cuando se agrega un punto con corriente 20.0 se termina parseando a 200, sacar opcion de 
poner floats con punto


La carga sigue entrando en corto cuando arranca la esp32, al no tener wifi elcorto es mas largopor
lo que se puede deducir que es porque nos e pone en 0 una salida del pwm. Fijarse si inicilizando a 3.3V elpin del adc en adcinit se soluciona

Deberia cambiar el limite de corriente que tengo en userinterface de 2000mA,podriaconfigurarlo para cada carga electronica

Podria agregar un sensado de tension en Vo para que si no hay tension no se activen las salida delpwm, asi evito que
cuando no halla tension el PID haga creceel dutycycle de formaautomatica


Para la carga de poca potencia al no poder calentar los focos para poca ptencia oscila, deberia usar resistneicas cermaicos en vez de focos

Pensar bien que pasa si cambio los tiempos en loq ue se ejecutan las tareas, elpid va a una velocidad y el sensado a otra, deberianser
al mismotiempo? podria cambiar ellugar de las funciones? sipongo sensado y actuacion con el mismo periodo?

Verificar que el datalogger funcion bien en segundo plano, fijarse si fucniona con la app cerrada y con lapc suspendida


Cuando leo todos los datos de la colaentre las task de adc y send data receiveSensorDataToUserInterface() no deberia quedarmesolo con el ultimo, o si?
Estos datos se envian a la app o solo se muestran en pantalla?(creo que solo son los que se muestra en lainterfaz)

Intentar si puedo cambiar el modo de funcionamiento de la carga CC-CP-CV-CR Cambiando el valor con el cual se realimenta.

Ver si puedo desactivar cargas y valores sensados para medir mas rapido con una cierta carga

Guardar otros valores de  V y P en el modulo de adc para luego suarlos en cargaelectronica y mandarselos a checkLimits()

Para eviat el corto al apagar la terminal es necesario
"""""cortar el trace DTR→EN (o RTS→EN, según la placa) en la board, o agregar un capacitor de ~1µF entre 
EN y GND para filtrar el pulso corto sin afectar un reset manual (que dura más tiempo)."""""
